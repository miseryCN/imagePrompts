name: Import verified Hangzhou atlas
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hangzhou atlas import branch ready'
